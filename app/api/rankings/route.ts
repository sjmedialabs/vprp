import { notImplementedResponse } from "@/lib/api-response";

export async function GET() {
  // TODO: Implement rankings fetching with filters (college, national, etc.)
  return notImplementedResponse("GET /api/rankings");
}
